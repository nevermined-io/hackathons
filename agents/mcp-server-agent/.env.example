# Nevermined - Builder (server owner, used by setup + server)
NVM_API_KEY=your-builder-api-key
NVM_ENVIRONMENT=staging

# These are created automatically by: poetry run python -m src.setup
# NVM_AGENT_ID=
# NVM_PLAN_ID=

# Nevermined - Subscriber (demo client, a different account)
NVM_SUBSCRIBER_API_KEY=your-subscriber-api-key

# LLM - OpenAI (for summarize and research tools)
OPENAI_API_KEY=sk-your-key
MODEL_ID=gpt-4o-mini

# Server
PORT=3000
